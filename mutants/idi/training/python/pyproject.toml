[project]
name = "idi-iann"
version = "0.1.0"
description = "Training helpers for Intelligent Daemon Interface lookup tables."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

